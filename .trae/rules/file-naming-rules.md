File Naming Rules
Format: snake_case (e.g., camera_driver.cpp, wifi_manager.h).
Clarity: File names must reflect the module functionality.
Header Guards: Use #pragma once instead of #ifndef guards for modern C++ style.